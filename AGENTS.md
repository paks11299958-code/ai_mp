# ai_mp 에이전트 지침

## 문맥·작업 목록

- **`CONTEXT.md`**(루트) — 도메인 용어집 + 정본 문서 지도. ★용어를 쓰기 전에 읽는다
- **`docs/adr/`** — 결정 기록. ★새 도구·모델을 제안하기 **전에** 훑는다(이미 탈락시킨 것 재조사 방지)
- **`docs/agents/domain.md`** — 위 둘을 읽고 고치는 규칙
- **`docs/agents/issue-tracker.md`** — 작업·버그 목록은 **GitHub Issues가 정본**
- **`docs/agents/triage-labels.md`** — 라벨 5종

## ★가장 먼저: 서버 구성 문서를 읽는다

전체 서버 인프라(GCP, Docker, Nginx, 도메인, 포트, 작업 이력)는 **이 저장소 밖**,
홈 디렉토리에 있다.

```
/home/paks11299958/work_index.md      # 서버 구성 인덱스 (약 500KB)
/home/paks11299958/work_lessons.md    # 과거 사고에서 뽑은 재발방지 교훈
```

★**반드시 절대경로로 접근할 것.** 이 파일들은 `~/ai_mp` **밖**에 있다.
저장소 안에서 `rg`/`find`를 상대경로로 돌리면 당연히 안 나온다.
"파일이 없다"고 결론짓기 전에 아래를 먼저 실행한다.

```sh
ls -la /home/paks11299958/work_index.md      # 존재 확인
sed -n '158,705p' /home/paks11299958/work_index.md   # §1~13 (항상 유효한 구성 정보)
```

★파일이 안 보인다고 해서 **"여기가 GCP2 서버가 아니다"라고 판단하지 말 것.**
서버 식별은 내부 IP 끝자리로 한다 — **.2=서버1(운영) / .3=서버2(에이전트·여기) / .9=서버3(GPU)**.
★서버3 GPU는 2026-08-24 `gcp3-new`(내부 `10.178.0.9`)로 **전수 교체**됐다. 옛 `ai-studio-gpu`/`.5`는
더 이상 존재하지 않는다. 온디맨드라 평소 `TERMINATED`인 것이 정상이다.
확인: `hostname -I` 또는
`curl -s -H "Metadata-Flavor: Google" http://metadata.google.internal/computeMetadata/v1/instance/name`
(서버2 = `dbzone-ai-20260524-072604`, 내부 10.178.0.3)
※서버2 외부 IP는 임시라 중지·시작마다 바뀐다. 외부 IP로 서버를 식별하지 말 것.

## work_index.md 읽는 법 (★전체 통독 금지)

500KB가 넘는다. **매번 전체를 읽지 마세요.** 아래 순서로 필요한 부분만 읽는다.

1. **작업 시작 전 필수** — `§1~13`(158~705줄)만 읽는다.
   서버 역할·아키텍처·포트·크론·배포 경로 등 **항상 유효한 구성 정보**다.
   특히 158줄의 `⚠️ 서버 역할 — 절대 규칙` 표는 반드시 확인한다.

2. **교훈·함정** — `/home/paks11299958/work_lessons.md`를 읽는다. 짧으니 전체를 읽는다.

3. **과거 작업 이력**(`§14`, 706줄~)은 **필요할 때만** 찾아 읽는다. 통독하지 않는다.
   ```sh
   grep -n "키워드" /home/paks11299958/work_index.md
   grep -n "^### 2026-08-2" /home/paks11299958/work_index.md   # 특정 날짜대 목록
   ```
   월별 과거분은 `/home/paks11299958/work_index_archive_*.md`에 분리돼 있다(같은 방식으로 grep).

## 기록할 때

- 새 작업 이력은 `§14` 최상단에 `### YYYY-MM-DD — 제목` 섹션으로 추가한다.
- ★**한 줄에 여러 날짜 요약을 이어붙이지 말 것.** 과거에 헤더 누적요약을 개행 없이
  연결하다 한 줄이 56,000자까지 불어나고, 그 사슬 안에만 존재하던 사흘치 이력이
  정식 섹션 없이 파묻힌 사고가 있었다(2026-07-28 정리).
- 재발방지 교훈이 나오면 이력에만 묻지 말고 `work_lessons.md`에도 한 줄 추가한다.

## 서버 역할 — 절대 규칙

- **여기는 서버2**(에이전트/개발 서버). **서버1은 운영 서버**다.
- 서버1 직접 수정·재시작·수동배포는 **명시적 요청이 있을 때만** 한다.
  단 서버2에서 커밋·푸시하면 **자동배포**되므로 "못 한다"고 오해하지 말 것.
- 푸시는 **master 브랜치만**(Vercel Production Branch = master).

## 비밀정보 취급

`.env`, `*.key`, `*.pem`, 인증·세션 파일, `secret-backup`, DB 덤프는
**읽지도 반환하지도 않는다.** 경로만 언급이 필요하면 값 없이 경로만 쓴다.

## 검증 원칙

- 빌드 통과 ≠ 검증. 배포 전 `npm run check`, 배포 후 `npm run smoke`까지 통과해야 "완료"다.
- "코드를 넣었다" ≠ "효과가 있다". 실제 적용값으로 실측한다.
- 번들 해시로 배포 여부를 판단하지 말 것(로컬/Vercel 해시는 원래 다르다).
  내용 grep + 운영 실동작으로 검증한다.
