# 프로젝트 컨텍스트

## 문맥·작업 목록

- **`CONTEXT.md`**(루트) — 도메인 용어집 + 정본 문서 지도. ★용어를 쓰기 전에 읽습니다
- **`docs/adr/`** — 결정 기록. ★새 도구·모델을 제안하기 **전에** 훑습니다(이미 탈락시킨 것 재조사 방지)
- **`docs/agents/domain.md`** — 위 둘을 읽고 고치는 규칙
- **`docs/agents/issue-tracker.md`** — 작업·버그 목록은 **GitHub Issues가 정본**
- **`docs/agents/triage-labels.md`** — 라벨 5종

## 서버 구성 인덱스

전체 서버 인프라(GCP, Docker, Nginx, 도메인, 포트, 작업 이력)는 아래 파일에 정리되어 있습니다:

**`/home/paks11299958/work_index.md`**

### 읽는 방법 (★전체 통독 금지)

이 파일은 500KB가 넘습니다. **매번 전체를 읽지 마세요.** 아래 순서로 필요한 부분만 읽습니다.

★**반드시 절대경로로 접근하세요.** 이 파일은 `~/ai_mp` **밖**에 있습니다.
저장소 안에서 상대경로로 `find`/`rg`를 돌리면 안 나옵니다(2026-08-22 코덱스 오판 사례).

1. **작업 시작 전 필수** — `§1~13`(158~705줄)만 읽습니다.
   서버 역할·아키텍처·포트·크론·배포 경로 등 **항상 유효한 구성 정보**입니다.
   특히 `§"⚠️ 서버 역할 — 절대 규칙"` 표는 반드시 확인하세요.

2. **교훈·함정** — `/home/paks11299958/work_lessons.md`를 읽습니다.
   과거 사고에서 뽑아낸 재발방지 규칙 모음입니다. 짧으니 전체를 읽으세요.

3. **과거 작업 이력**(`§14`)은 **필요할 때만** 찾아 읽습니다. 통독하지 마세요.
   ```sh
   grep -n "키워드" ~/work_index.md              # 예: grep -n "토스봇" ~/work_index.md
   grep -n "^### 2026-07-2" ~/work_index.md      # 특정 날짜대 섹션 목록
   ```
   월별 과거분은 `work_index_archive_*.md`에 분리돼 있습니다(같은 방식으로 grep).

4. RAG 검색도 가능합니다(`rag/rag_search.py`, work_index 임베딩 완료).

### 기록할 때

- 새 작업 이력은 `§14` 최상단에 `### YYYY-MM-DD — 제목` 섹션으로 추가합니다.
- ★**한 줄에 여러 날짜 요약을 이어붙이지 마세요.** 과거에 헤더 누적요약을 개행 없이
  연결하다 한 줄이 56,000자까지 불어나고, 그 사슬 안에만 존재하던 사흘치 이력이
  정식 섹션 없이 파묻힌 사고가 있었습니다(2026-07-28 정리).
- 재발방지 교훈이 나오면 이력에만 묻지 말고 `work_lessons.md`에도 한 줄 추가하세요.

## Agent skills

### Issue tracker

작업과 PRD는 GitHub Issues에서 관리합니다. 자세한 규칙은 `docs/agents/issue-tracker.md`를 따릅니다.

### Triage labels

기본 5종 라벨을 사용합니다. 자세한 매핑은 `docs/agents/triage-labels.md`를 따릅니다.

### Domain docs

단일 문맥 구조를 사용합니다. `CONTEXT.md`와 `docs/adr/`를 기준으로 합니다. 자세한 규칙은 `docs/agents/domain.md`를 따릅니다.
