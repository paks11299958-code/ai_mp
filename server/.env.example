# ── 서버 설정 ──────────────────────────────
PORT=3020
NODE_ENV=production

# ── DB 커넥션 풀 ──────────────────────────
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/DB_NAME
POOL_MIN=2
POOL_MAX=10

# ── CORS 허용 오리진 (쉼표 구분) ─────────
CORS_ORIGIN=https://aichat.dbzone.kr,https://ploppy.dbzone.kr
