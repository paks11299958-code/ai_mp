# 도결 선생 진입 화면 — 사주 랜딩형 개편 (SPEC)

> 2026-08-26 사장 지시. 대상 = **도결(道潔) 선생 한 명만**.
> 다른 페르소나는 기존 진입 시트를 그대로 쓴다.

## 왜 하는가

지금 페르소나에 들어가면 **채팅창이 먼저** 보인다. 사장 지적:
*"뭘 할 수 있는지 안 보이고 분위기도 안 산다."*
사주는 분위기 자체가 상품인데 일반 챗봇과 구분이 안 됐다.

→ 도결 선생을 클릭하면 **사주 사이트 같은 큰 랜딩 화면**이 먼저 뜨고,
거기서 기능을 고르거나 채팅으로 들어간다.

## 건드릴 파일 (★이 밖은 금지)

| 파일 | 할 일 |
|---|---|
| `frontend/components/PersonaEntrySheet.tsx` | 도결 분기 추가. **기존 경로는 한 줄도 바꾸지 말 것** |
| `frontend/components/persona/SajuEntry.tsx` (신규) | 사주 랜딩 본체 |
| `frontend/components/persona/sajuHero.ts` (신규) | 히어로 캔버스 로직 |

★`App.tsx` 는 **수정하지 않는다.** `PersonaEntrySheet` 안에서 `guide.title` 이
`도결` 로 시작하면 새 화면을, 아니면 기존 화면을 렌더한다.
★★`App.tsx` 를 건드리면 **전 화면 백지 사고**가 재발할 수 있다(2026-07-29 실사고:
`useCallback` 의존성 TDZ, tsc·안전검사 모두 통과하고 실렌더에서만 터졌다).

## 기존 계약 (반드시 유지)

```ts
interface PersonaEntryGuide {
  title: string;            // "도결(道潔) 선생"
  desc: string;             // introText
  features?: { key; name; icon; accent; bg }[];
  usesBirthInfo?: boolean;
  imageUrl?: string;
  autoRunFeatureKey?: string;
}
// props: { guide, onClose, onStart(runKey?), onFeature(key) }
```
- `onStart()` = 채팅 진입, `onFeature(key)` = 해당 기능 실행. **이 시그니처를 바꾸지 말 것.**
- ✕·배경 클릭·Esc = `onClose()`. 스크롤 복원 동작 유지.

## 화면 구성

참고 레이아웃(사장 제공 예시 사이트) — **로그인·회원가입 영역은 넣지 않는다.**

```
┌──────────────────────────────────────────────────────┐
│  좌: 카피 + 기능 카드 + CTA   │   우: 히어로 캔버스   │
├──────────────────────────────────────────────────────┤
│  하단: 도결 선생 기능 목록(가로 카드)                 │
└──────────────────────────────────────────────────────┘
```

### 히어로 연출 (★사장 지시의 핵심)

**역술가의 오래된 책 → 연기가 피어오르며 책이 사라짐 → 한국 호랑이가 천천히 나타남**

- 이미지 2장이 이미 있다: `/saju/book.png`, `/saju/tiger.png` (public, 1216×832)
- **Canvas 2D 로 연기·안개 + 크로스페이드**를 그린다. 외부 라이브러리 금지.

★**두 이미지에 이미 실제 연기가 찍혀 있다**(책=향로 연기, 호랑이=주변에 감도는 연기).
캔버스가 그리는 연기는 **그 사진의 연기와 이어붙이는** 역할이다 — 처음부터 다 그리려 하지 말 것.
책의 향로는 화면 중앙 상단, 호랑이의 연기는 좌우에 있으니 발생 위치를 거기에 맞춘다.

**타임라인** (약 6초, 1회 재생 후 호랑이 상태로 정지)

| 구간 | 연출 |
|---|---|
| `0 ~ 1.5s` | 책만. 향로 위에서 **연기가 서서히 짙어지기 시작** |
| `1.5 ~ 3.5s` | **연기(피어오름)** — 아래에서 위로 솟구쳐 책을 감싼다. 책이 페이드아웃 |
| `3.5 ~ 5.5s` | **안개(깔림)로 전환** — 화면 전체에 옆으로 퍼지고, 그 속에서 호랑이가 서서히 드러난다 |
| `5.5s ~` | 호랑이 유지. 안개만 은은하게 남아 천천히 흐른다 |

흐름은 **"책이 연기에 삼켜지고 → 안개가 걷히며 호랑이가 나타난다"** 이다(2026-08-26 사장 확정).

**연기와 안개는 성질이 다르다 — 둘을 구분해 그릴 것**
- **연기**: 발생점(향로)에서 **위로 상승**. 입자가 작게 시작해 커지며 옅어진다.
  좌우 흔들림(sin 파동)을 주고, 상승할수록 흔들림 폭을 키운다.
- **안개**: **가로로 느리게 흐른다**. 큰 반투명 덩어리 여러 겹이 서로 다른 속도로 움직여
  깊이를 만든다. 상승은 거의 없다.
- 둘 다 `radialGradient` 로 가장자리를 흐리게 하고, `globalCompositeOperation` 은
  `'lighter'` 가 아니라 기본값 또는 `'screen'` 을 쓴다(먹색 배경에서 `lighter` 는 탄다).
- 입자 수는 PC 40~60, 모바일 20~30 정도. **화면 밖이면 rAF 정지**.

- ★`prefers-reduced-motion` 이면 **캔버스 없이 호랑이 이미지만** 정적으로 보인다.

### 카피
- 상단 배지: `57년 내공`
- 제목: `도결(道潔) 선생`
- 부제: DB `introText` 를 그대로(지어내지 말 것)
- CTA: `도결 선생과 대화하기`(→`onStart()`) / 부CTA: `무엇을 볼 수 있나`(기능 목록으로 스크롤)

### 기능 카드 — ★DB 값만 쓴다
`guide.features` 배열을 그대로 렌더한다. **없는 기능을 만들어 넣지 말 것.**
카드 클릭 = `onFeature(key)`.

참고로 도결 선생의 실제 퀵메뉴는 다음과 같다(문구 창작 금지, 화면 문구로 쓸 때 이대로):
📅 운세(오늘의 운세·이달의 흐름·올해의 비결) · 💰 재물(재물 흐름·사업/성취) ·
❤️ 인연(연애 운세·인연 궁합) · 🕉️ 전생 · 🌙 해몽 · 🔮 관상 · 🖐 손금 · 🤝 우정

## 톤 — 사주 사이트 분위기

```
바탕   #0d0b0a ~ #14100e (먹색)
금박   #c9a227 / #e8cc6a (제목·강조)
주홍   #b8352c (포인트, 2026 병오년의 붉은 기운)
글씨   #f0e9dc (본문) / #a89a86 (보조)
serif  "Noto Serif KR" — 제목은 명조로
```
★기존 사이트의 밝은 톤·핑크 계열을 쓰지 말 것. 어두운 먹색이 기본이다.

## 절대 지킬 것

1. **`App.tsx` 수정 금지.** 분기는 `PersonaEntrySheet` 안에서.
2. **다른 페르소나 동작 무변경.** 도결이 아니면 기존 JSX 그대로 반환.
3. **로그인·회원가입 UI 금지**(사장 지시). 진입 시트는 로그인 상태와 무관하다.
4. **외부 CDN·라이브러리 금지.** Canvas 2D 로 직접.
5. **canvas 최대 1개**, 화면 밖이면 `IntersectionObserver` 로 rAF 정지, `devicePixelRatio` 대응.
6. **`prefers-reduced-motion`** 이면 애니메이션 없이 정적 대체.
7. **모바일 360px 에서 가로 스크롤 0.** 모바일 분기 **820px**.
8. `img` 에 **`height:auto`** (HTML width/height 속성이 남으면 `aspect-ratio` 가 무력화돼 잘린다).
9. **사실 창작 금지** — 연락처·가격·실적·후기·통계를 지어내지 말 것. DB 값과 위 문구만.
10. 접근성: ✕ 버튼에 `aria-label`, Esc 닫기, 포커스 트랩 유지(기존 동작 보존).

## 완료 조건 (체커가 확인할 것)

- 데스크톱 1440×900 · 모바일 390×844 에서 가로 넘침 0, 콘솔 오류 0
- 도결 선생 진입 시 새 화면, **다른 페르소나는 기존 화면** (둘 다 확인)
- `onStart`/`onFeature`/`onClose` 가 기존과 동일하게 동작
- 히어로가 **책 → 연기(상승) → 안개(가로 흐름) → 호랑이** 순으로 재생된다.
  ★연기와 안개가 **같은 움직임이면 안 된다** — 연기는 위로, 안개는 옆으로.
  중간 프레임을 캡처해 책이 사라지고 호랑이가 드러나는 전환이 실제로 보이는지 확인할 것.
- 모션 감소 설정에서는 캔버스 없이 호랑이만 정적으로 표시
- `npm run check` 와 프런트 빌드 통과
