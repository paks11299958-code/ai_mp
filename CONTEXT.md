# CONTEXT — 도메인 용어집 + 정본 문서 지도

Codex와 Claude가 **같은 말을 쓰기 위한** 문서다. 읽고 고치는 규칙은
[docs/agents/domain.md](docs/agents/domain.md)에 있다.

★**이 파일은 정본이 아니라 지도다.** 다른 문서의 내용을 복사해 오지 않는다.
"이 개념은 어느 문서가 정본인가"만 가리킨다.

---

## 정본 문서 지도

| 무엇 | 어디 |
|---|---|
| 기술 스택·디렉터리 구조·화면 흐름 | `README.md` |
| 기능별 상세 설계·이력 (44개) | `doc/features/*.md` |
| DB 스키마 | `doc/db_schema.md` |
| 결정 기록 | `docs/adr/` |
| 이슈 트래커 운영 | [docs/agents/issue-tracker.md](docs/agents/issue-tracker.md) |
| 라벨 5종 | [docs/agents/triage-labels.md](docs/agents/triage-labels.md) |
| 에이전트 지침 | `AGENTS.md`(Codex) / `CLAUDE.md`(Claude) |
| ★서버 인프라·포트·크론·작업 이력 | `/home/paks11299958/work_index.md` **저장소 밖** |
| ★재발방지 교훈 | `/home/paks11299958/work_lessons.md` **저장소 밖** |

★인프라 문서 2개는 `~/ai_mp` **밖**에 있다. 반드시 절대경로로 접근한다.

---

## 서버 (★역할을 혼동하지 말 것)

내부 IP 끝자리로 식별한다. 외부 IP는 임시라 재부팅마다 바뀌므로 **식별에 쓰지 않는다.**

| 용어 | 뜻 |
|---|---|
| **서버1** | 운영 서버(`10.178.0.2`). shared-api·운영 DB. 직접 수정은 명시적 요청이 있을 때만 |
| **서버2** | 에이전트·개발 서버(`10.178.0.3`). **여기**. 커밋·푸시하면 자동배포된다 |
| **서버3** | GPU 서버(`gcp3-new`, `10.178.0.9`). 유휴 30분·최대 4시간 자동 종료 |

---

## 용어

★아래 표기를 그대로 쓴다. 동의어로 흘리면 이슈가 중복 등록되고 검색이 안 된다.

| 쓴다 | 쓰지 않는다 | 뜻 · 정본 |
|---|---|---|
| **쇼츠** | 숏츠, 쇼트폼 | 유튜브 세로 영상 자동 제작. `doc/features/shorts_maker.md` |
| **AI 스튜디오** | ComfyUI, GPU 스튜디오 | 서버3 이미지·영상 생성. `doc/features/ai_studio_gpu.md`, `ai_studio_ops.md` |
| **AI 아바타** | 3D 캐릭터 메뉴 | 사진 기반 2.5D 아바타 제작·검수·게시 어드민. `doc/features/ai_avatar_admin.md` |
| **페르소나** | 캐릭터, 봇 | 다중 AI 채팅의 대화 상대. `README.md` |
| **기능 키** | 메뉴 키, 피처 키 | 기능 식별자. ★등록처 4곳 중 하나만 빠져도 딥링크가 조용히 채팅으로 폴백한다 |
| **토스봇** | 트레이더, 자동매매봇 | 자동매매. `doc/features/toss_auto_trading.md` |
| **인버스** | 스캘핑봇 | 인버스 전략. `doc/features/inverse_trader.md` |
| **개발AI 콘솔** | devai, 개발콘솔 | 어드민 명세→개발→승인 파이프라인. `doc/features/devai_console.md` |
| **허드(Herdr)** | 헤르메스 파이프라인 | 텔레그램 원격 개발. 헤르메스는 **텔레그램 봇 쪽**을 가리킨다 |
| **확대 후보정** | 업스케일, 업스케일러 | 생성 후 해상도·디테일 보정 |
| **체험계정** | 게스트, guest | 7일 후 삭제되는 임시 계정 |
| **ThreeUI 카탈로그** | ThreeUI 전체설치 | 홈페이지 만들기에 넣을 검증된 Community 3D 컴포넌트 허용 목록. `doc/features/homepage_threeui.md` |

★새 도메인 개념을 만들었으면 **그 자리에서 이 표에 추가한다.**

---

## 검증 기준 (용어의 뜻을 고정해 둔다)

이 프로젝트에서 반복해서 어긋났던 말들이다.

| 말 | 실제 뜻 |
|---|---|
| **"완료"** | 배포 전 `npm run check` + 배포 후 `npm run smoke` 통과. 빌드 통과는 완료가 아니다 |
| **"적용했다"** | 실제 적용값으로 실측했다. "코드를 넣었다"는 적용이 아니다 |
| **"배포됐다"** | 내용 grep + 운영 실동작 확인. ★번들 해시로 판단하지 않는다 |
| **"감시하고 있다"** | 상태파일까지 확인했다. "감시가 있다" ≠ "작동한다" |
| **"알림이 갔다"** | 조치까지 됐다는 뜻이 **아니다** |
